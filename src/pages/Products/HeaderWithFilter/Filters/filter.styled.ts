import styled from "styled-components";

export const FilterBox = styled.div`
  display: flex;
  gap: 32px;

  & > button {
    display: flex;
    align-items: center;
    gap: 8px;
    font-family: Poppins;
    font-size: 16px;
    color: var(--darkBlue);
  }
`;
