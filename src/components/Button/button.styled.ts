import styled from "styled-components";

export const StyledButton = styled.button`
  color: white;
  background-color: var(--red);
  padding: 10px 0;
  border-radius: 10px;
  text-align: center;
  font-weight: 400;
  font-size: 14px;
  line-height: 100%;
`;
